(version 0.9.7)

(build
 (progn
  (when
   %{pkg-self:dev}
   (run dune subst))
  (run dune build -p %{pkg-self:name} -j %{jobs} @install)))

(depends ocaml dune cmdliner ctypes ctypes-foreign ppx_deriving)

(source
 (fetch
  (url https://github.com/roddyyaga/pg_query-ocaml/archive/0.9.7.tar.gz)
  (checksum md5=0f60f8c91c556be3f3a7e8f3f8305bb2)))
